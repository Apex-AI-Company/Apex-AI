---
import { Image } from "astro:assets"

export interface Props {
  phoneNumber: string;
  prefilledMessage?: string;
}

const { 
  phoneNumber, 
  prefilledMessage = "Hello! I'd like to chat about your services." 
} = Astro.props;

const whatsappLink = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(prefilledMessage)}`;
---

<div id="whatsapp-widget" class="fixed bottom-4 left-4 z-[3001] print:hidden">
  <button
    id="whatsapp-toggle"
    aria-label="Open WhatsApp chat"
    aria-expanded="false"
    aria-controls="whatsapp-chat-popup"
  >
    <Image height={50} width={50} src="/icons/WhatsApp.svg.webp" alt="Whatsapp" />
  </button>

  <!-- Chat Tab/Popup -->
  <div
    id="whatsapp-chat-popup"
    class="absolute bottom-full mb-3 w-64 sm:w-72 bg-white rounded-lg shadow-xl p-4 origin-bottom-left opacity-0 transform scale-90 translate-y-2 pointer-events-none transition-all duration-200 ease-out"
    role="dialog"
    aria-modal="true"
    aria-labelledby="whatsapp-chat-title"
  >
    <button 
      id="whatsapp-close"
      class="absolute top-2 right-2 text-gray-500 hover:text-gray-700 p-1 rounded-full focus:outline-none focus:ring-2 focus:ring-gray-400 w-6 h-6 flex items-center justify-center text-lg font-bold" 
      aria-label="Close WhatsApp chat popup"
    >
      ×
    </button>
    <p id="whatsapp-chat-title" class="text-md font-semibold text-gray-800 mb-3 pr-6">
      Chat with us on WhatsApp
    </p>
    <a
      href={whatsappLink}
      target="_blank"
      rel="noopener noreferrer"
      class="w-full flex items-center justify-center bg-green-500 hover:bg-green-600 text-white font-semibold py-2 px-4 rounded-md text-sm transition-colors focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-opacity-50"
    >
      <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 24 24">
        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893A11.821 11.821 0 0020.885 3.485"/>
      </svg>
      Chat on WhatsApp
    </a>
  </div>
</div>

<script>
  // WhatsApp popup functionality
  document.addEventListener('DOMContentLoaded', function() {
    const toggleButton = document.getElementById('whatsapp-toggle');
    const popup = document.getElementById('whatsapp-chat-popup');
    const closeButton = document.getElementById('whatsapp-close');
    const widget = document.getElementById('whatsapp-widget');
    
    let isOpen = false;
    
    function openPopup() {
      isOpen = true;
      popup.classList.remove('opacity-0', 'scale-90', 'translate-y-2', 'pointer-events-none');
      popup.classList.add('opacity-100', 'scale-100', 'translate-y-0');
      toggleButton.setAttribute('aria-expanded', 'true');
    }
    
    function closePopup() {
      isOpen = false;
      popup.classList.remove('opacity-100', 'scale-100', 'translate-y-0');
      popup.classList.add('opacity-0', 'scale-90', 'translate-y-2', 'pointer-events-none');
      toggleButton.setAttribute('aria-expanded', 'false');
    }
    
    // Toggle popup when button is clicked
    toggleButton.addEventListener('click', function(e) {
      e.stopPropagation();
      if (isOpen) {
        closePopup();
      } else {
        openPopup();
      }
    });
    
    // Close popup when close button is clicked
    closeButton.addEventListener('click', function(e) {
      e.stopPropagation();
      closePopup();
    });
    
    // Close popup when clicking outside
    document.addEventListener('click', function(e) {
      if (isOpen && !widget.contains(e.target)) {
        closePopup();
      }
    });
    
    // Close popup on escape key
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && isOpen) {
        closePopup();
      }
    });
    
    // Prevent popup from closing when clicking inside it
    popup.addEventListener('click', function(e) {
      e.stopPropagation();
    });
  });
</script>